<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cr.ac.utn.appmovil.rooms">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Restapi">

        <!-- La app SIEMPRE arranca en MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tus activities del examen (sin launcher) -->
        <activity
            android:name=".knu_AuthenticationActivity"
            android:exported="false" />

        <activity
            android:name=".knu_RoomListActivity"
            android:exported="false" />

        <activity
            android:name=".knu_RoomActivity"
            android:exported="false" />

        <!-- Si tu proyecto tiene otras activities de ejemplo, las dejas tal cual -->
    </application>

</manifest>
